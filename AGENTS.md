# AGENTS.md

## 项目定位

本项目是“稻田智研平台”，面向研究所研究人员、项目维护人员和数据管理员，核心目标是建设水稻试验田数据可视化与分析平台。

系统重点解决三类问题：

1. 数据难整理：Excel、GeoJSON、PDF、气象、光谱等数据来源不统一，需要解析、校验、标准化和质量报告。
2. 地块难定位：需要结合地图和地块边界，快速定位地块并查看指标状态。
3. 分析难复用：需要从一开始保持清晰架构、接口规范、数据模型和文档体系。

## 技术栈约束

不要引入未讨论的新技术。默认遵循以下技术方案：

- 前端：Vue 3、TypeScript、Vite、Vue Router、Pinia、Ant Design Vue、ECharts、CesiumJS、Axios。
- 后端：FastAPI、Pydantic、pandas、openpyxl、SQLAlchemy、Alembic、Pytest。
- MVP 数据层：标准化 JSON / CSV / 可选 SQLite。
- 正式数据层：PostgreSQL + PostGIS。
- 部署：Nginx、Docker Compose。

MVP 阶段暂缓 Tailwind CSS、复杂三维能力、权限系统、自动报告、多项目管理和复杂空间分析。

## MVP 边界

第一版优先完成：

1. Excel 数据解析。
2. 地块 GeoJSON 加载。
3. Cesium 地图展示。
4. 指标筛选与地块着色。
5. 点击地块查看趋势。
6. 导入质量报告。

不要在没有确认的情况下提前实现第二阶段能力。

## 数据治理原则

所有指标进入系统前必须先进入指标字典。Excel 解析后统一转换为长表结构。前端不得直接依赖 Excel 原始结构。

核心数据必须支持来源追溯，至少保留：

- 地块编号；
- 指标编码；
- 指标值；
- 观测时间；
- 导入批次；
- 来源文件；
- 来源工作表；
- 来源单元格；
- 数据质量标记。

每次导入都必须生成质量报告，不能隐藏缺失值、异常值、未匹配地块或解析错误。

## 临时数据原则

临时样例数据只用于理解数据结构和验证导入规则，不作为长期仓库内容。不要在业务代码、测试或文档规范中硬编码临时样例目录路径。

如果需要保留样例数据带来的结论，应沉淀为指标字典、数据模型、导入契约、质量规则或测试夹具设计，而不是依赖临时目录本身。

## 架构约定

后端路由函数只处理 HTTP 输入输出，业务逻辑放在 service 层。请求、响应和数据结构使用 Pydantic schema 约束。

前端通过 API 消费标准化数据，不直接解析原始 Excel、GeoJSON 或 PDF。

地图第一版只承担底图、地块边界、地块着色、点击详情、定位和图表联动，不做复杂三维能力。

## 文档同步

开发前先写清楚功能目标、数据字段、接口草案和页面结构。

新增或修改以下内容时，必须同步更新文档：

- 数据字段；
- 指标编码；
- 导入规则；
- API 响应结构；
- 页面结构；
- 设计规范；
- 部署方式。

优先更新对应文档：

- `docs/DATA_MODEL.md`
- `docs/METRIC_DICTIONARY.md`
- `docs/API.md`
- `docs/DESIGN_SYSTEM.md`
- `docs/IMPORT_GUIDE.md`
- `docs/CHANGELOG.md`

每次有实际更新时，必须同步更新 `docs/CHANGELOG.md`。变更记录按日期归档，使用中文说明新增、修改、修复、文档更新和验证情况。

每次完成更新并通过相应验证后，必须执行 Git 提交。提交前检查暂存范围，避免提交虚拟环境、缓存、临时样例数据或无关文件。

## 交互与设计原则

界面中文优先，适合科研数据分析场景。页面应清晰、易用、高效，地图、图表、表格风格统一，信息密度适中。

不要做偏营销化的页面。优先做真实可用的分析工作台、筛选器、地图、图表、表格和质量报告。

## 协作原则

涉及项目规划、技术栈、数据模型、目录结构、MVP 边界或导入契约的修改，先提出建议并等待用户确认，不要直接修改。

路径不是最短时，要指出更简单的方案。目标不清晰时，先讨论再实现。
